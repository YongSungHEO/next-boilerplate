import React from 'react';

type Props = {
    goBack: () => void,
};

const SampleContainer: React.FC<Props> = ({ goBack }) => {

    return (
        <></>
    );
};

export default SampleContainer;
